package org.aslib;

/**
 * The AuctionStatus enum represents the possible states of an auction.
 */
public enum AuctionStatus {
    NEW,    // Auction is newly created
    OPEN,   // Auction is currently open for bidding
    CLOSED  // Auction is closed
}
